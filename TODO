# TODO

show wiki (description/instructions) history to logged in user
clean up app screenshots view
clean up App show view
integrate Hallo/Markdown
allow user voting

lock down screenshots#index

associate bug reports with Apps (:has_many_through => WineAppProblems )
populate bug reports table with mock data

create custom devise views
add landing page
allow user voting via ajax
allow user subscriptions to App data for updates
identify votes as coming from non-registered users, don't allow those users to vote more than once
implement Map(Reduce()) jQuery lookup for searching

